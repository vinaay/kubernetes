apiVersion: v1
kind: ConfigMap
metadata:
  name: game-demo
data:
  # property-like keys; each key maps to a simple value
  player_initial_lives: "4"
  ui_properties_file_name: "6"

  # file-like keys
  game.properties: |
    enemy.types=aliens,monsters
    player.maximum-lives=5   
    newvalues=3 
  user-interface.properties: |
    color.good=purple
    color.bad=yellow
    allow.textmode=true  